<%= form_for(@eeg) do |f| %>
  <% if @eeg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@eeg.errors.count, "error") %> prohibited this eeg from being saved:</h2>

      <ul>
      <% @eeg.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :event %><br>
    <%= f.text_field :event %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
